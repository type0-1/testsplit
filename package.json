{
  "scripts": {
    "test:unit": "jest --config jest.unit.config.js",
    "test:integration": "jest --config jest.integration.config.js",
    "test": "npm run test:unit",
    "build": "tsc",
    "docker:build": "docker compose build",
    "docker:test": "docker compose run --rm testsplit npm test",
    "docker:shell": "docker compose run --rm testsplit bash"
  },
  "devDependencies": {
    "@types/jest": "30.0.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "25.0.3",
    "jest": "30.2.0",
    "ts-jest": "29.4.6",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "@xmldom/xmldom": "0.9.5",
    "fast-xml-parser": "5.3.3",
    "js-yaml": "^4.1.1",
    "yaml": "^2.8.2"
  }
}
